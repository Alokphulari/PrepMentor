import ResumeAnalysisPanel from "../components/ResumeAnalysisPanel";

// Compatibility entry point; ResumeBuilder remains the routed resume studio.
export default function Resume() {
  return <ResumeAnalysisPanel />;
}
