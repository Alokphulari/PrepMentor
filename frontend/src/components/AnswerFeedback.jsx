import InterviewAvatar from "./InterviewAvatar";
import { ArrowRight, Volume2, VolumeX } from "lucide-react";
export default function AnswerFeedback({ evaluation, onNext, speaking = false, speechError, onReplay, onStop }) {
  return <section className="surface-card space-y-5 rounded-3xl p-6 sm:p-9" aria-label="Answer evaluation"><InterviewAvatar feedback speaking={speaking}/><div className="flex flex-wrap items-center gap-3"><button type="button" onClick={speaking ? onStop : onReplay} className="inline-flex items-center gap-2 rounded-xl border border-indigo-200 px-4 py-2 text-sm font-bold text-indigo-600 dark:border-indigo-800 dark:text-indigo-300">{speaking ? <><VolumeX size={16}/> Stop feedback</> : <><Volume2 size={16}/> Replay feedback</>}</button><p className="text-xs text-gray-500">Ava reads your score, feedback and key improvements.</p></div>{speechError && <p role="alert" className="text-sm text-amber-700">{speechError}</p>}<div className="flex items-center justify-between gap-4"><h2 className="text-xl font-extrabold">Answer evaluation</h2><span className="text-2xl font-black text-indigo-600 dark:text-indigo-300">{evaluation.score}/100</span></div><p className="text-xs text-gray-500">Evaluation source: {evaluation.provider === "local" || evaluation.fallbackUsed ? "Local Backup" : evaluation.provider === "gemini" ? "Gemini" : evaluation.source || "Local Backup"} ? {evaluation.source}</p><dl className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["technicalAccuracy","Technical accuracy"],["relevance","Relevance"],["communication","Communication"],["reasoning","Reasoning"]].map(([key,label])=><div key={key} className="rounded-2xl bg-indigo-50 p-4 dark:bg-indigo-950/40"><dt className="text-xs text-gray-500 dark:text-gray-400">{key === "technicalAccuracy" && evaluation.provider === "local" ? "Concept coverage" : label}</dt><dd className="mt-1 font-bold">{evaluation[key] ?? "Not assessed"}</dd></div>)}</dl>{[["strengths","What you did well"],["improvements","Improve"]].map(([key,label])=>evaluation[key]?.length>0&&<div key={key}><h3 className="font-bold">{label}</h3><ul className="mt-2 list-disc space-y-1 pl-5 text-sm leading-6 text-gray-600 dark:text-gray-300">{evaluation[key].map((item)=><li key={item}>{item}</li>)}</ul></div>)}<div><h3 className="font-bold">Feedback</h3><p className="mt-2 text-sm leading-7 text-gray-600 dark:text-gray-300">{evaluation.feedback}</p></div><div className="rounded-2xl border border-indigo-100 bg-indigo-50/50 p-5 dark:border-indigo-900 dark:bg-indigo-950/30"><h3 className="font-bold text-indigo-700 dark:text-indigo-300">{evaluation.provider === "local" ? "Answer review guide" : "Strong sample answer"}</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-7">{evaluation.idealAnswer || "A sample answer is unavailable while semantic AI evaluation is offline."}</p></div><button type="button" onClick={onNext} className="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white hover:bg-indigo-700">Next Question <ArrowRight size={17}/></button></section>;
}
