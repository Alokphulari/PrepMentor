# Public configuration only. NEVER add provider keys or database credentials here.
# Leave VITE_API_BASE_URL blank to use explicit local/offline practice.
# To use the included backend locally, set:
VITE_API_BASE_URL=http://localhost:4000
# Optional public OAuth web client ID. Must match backend GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
